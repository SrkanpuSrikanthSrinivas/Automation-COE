---
name: "toklite"
tagline: "A zero-dependency Node.js proxy that cuts LLM token usage."
category: "AI for testing"
status: stable
platforms: [Node.js, npm]
install: "npm install toklite"
maintainers: [srikanth]
featured: true
order: 5
---

## What it does

toklite sits between your code and an LLM provider and reduces the tokens you send. It has no runtime dependencies, which keeps it easy to drop into test pipelines that call models.
